{
  "name": "wellphone-model-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file=.env app/server.js",
    "dev": "node --watch --env-file=.env app/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
